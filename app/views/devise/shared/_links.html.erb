<%- if controller_name != 'sessions' %>
  <div class="justitia-extra-links justitita-box">
    <div><h4>Already have an account?</h4></div>
    <div><%= link_to "Log in", new_session_path(resource_name), class: "btn btn-primary" %></div>
  </div>
<% end -%>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <div class="justitia-extra-links justitita-box">
    <div><h4>Don't have an account yet?</h4></div>
    <div><%= link_to "Sign up", new_registration_path(resource_name), class: "btn btn-primary" %></div>
  </div>
<% end -%>

<%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
  <div class="justitia-extra-links justitita-box">
    <div><h4>Forgot your password?</h4></div>
    <div><%= link_to "Reset", new_password_path(resource_name), class: "btn btn-primary" %></div>
  </div>
<% end -%>

<%- if devise_mapping.confirmable? && controller_name != 'confirmations' %>
  <div class="justitia-extra-links justitita-box">
    <div><h4>Didn't receive confirmation instructions?</h4></div>
    <div><%= link_to "Resend email", new_confirmation_path(resource_name), class: "btn btn-primary" %></div>
  </div>
<% end -%>

<%- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
  <%= link_to "Didn't receive unlock instructions?", new_unlock_path(resource_name) %><br />
<% end -%>

<%- if devise_mapping.omniauthable? %>
  <%- resource_class.omniauth_providers.each do |provider| %>
    <%= link_to "Sign in with #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider) %><br />
  <% end -%>
<% end -%>
